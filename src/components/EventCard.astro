---
const { event } = Astro.props;
---

<div
  class="relative group h-full overflow-hidden rounded-2xl bg-neutral-900 border border-white/5 hover:border-brand/30 transition-all duration-500"
>
  <!-- Image Placeholder / Gradient Overlay -->
  <div
    class="absolute inset-0 bg-gradient-to-b from-transparent via-black/50 to-black/90 z-10"
  >
  </div>

  <!-- Optional: If you have images for events, put them here. For now using a pattern -->
  <div
    class="absolute inset-0 bg-[url('/images/pattern.png')] opacity-20 group-hover:scale-110 transition-transform duration-700 z-0"
  >
  </div>

  <div class="relative z-20 p-8 flex flex-col h-full justify-end min-h-[320px]">
    <div class="mb-auto">
      <span
        class="inline-block px-3 py-1 bg-brand text-black text-xs font-bold rounded-full mb-4 uppercase tracking-wider transform -translate-y-2 opacity-0 group-hover:translate-y-0 group-hover:opacity-100 transition-all duration-300"
      >
        Imperdible
      </span>
    </div>

    <div
      class="transform translate-y-4 group-hover:translate-y-0 transition-transform duration-300"
    >
      <div class="flex items-center gap-3 text-brand mb-2">
        <svg
          xmlns="http://www.w3.org/2000/svg"
          class="h-5 w-5"
          fill="none"
          viewBox="0 0 24 24"
          stroke="currentColor"
        >
          <path
            stroke-linecap="round"
            stroke-linejoin="round"
            stroke-width="2"
            d="M8 7V3m8 4V3m-9 8h10M5 21h14a2 2 0 002-2V7a2 2 0 00-2-2H5a2 2 0 00-2 2v12a2 2 0 002 2z"
          ></path>
        </svg>
        <span class="font-bold uppercase tracking-widest text-sm"
          >{event.date}</span
        >
      </div>

      <h3
        class="text-3xl font-serif font-bold text-white mb-4 leading-tight group-hover:text-brand transition-colors duration-300"
      >
        {event.title}
      </h3>

      <div
        class="h-0 overflow-hidden group-hover:h-auto transition-all duration-300 opacity-0 group-hover:opacity-100"
      >
        <p class="text-gray-400 text-sm mb-4">
          ¡Reserva tu mesa con anticipación y no te quedes fuera de esta gran
          noche!
        </p>
        <span class="text-white text-sm font-bold border-b border-brand pb-0.5"
          >Reservar ahora</span
        >
      </div>
    </div>
  </div>
</div>
